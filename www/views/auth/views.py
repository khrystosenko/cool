from ..utils.views import JSONResponse

def signup(request):
	return JSONResponse({'status': 'ok'})

def login(request):
	return JSONResponse({'status': 'ok'})

def logout(request):
	return JSONResponse({'status': 'ok'})